name: Heart Disease Model Training Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'MLProject/**'
  workflow_dispatch:

jobs:
  training-pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Clone repository
        uses: actions/checkout@v3

      - name: Prepare Python environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install required packages
        run: |
          python -m pip install --upgrade pip
          pip install mlflow==2.13.0 scikit-learn==1.4.2 pandas==2.2.2 joblib==1.4.2 matplotlib==3.8.4 dagshub


      - name: Train model via MLflow Run
        env:
          MLFLOW_TRACKING_USERNAME: "gloryabacas"
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_USER_TOKEN }}
          MLFLOW_ALLOW_FILE_STORE: "true"
        run: |
          cd MLProject
          export MLFLOW_TRACKING_URI=https://dagshub.com/gloryabacas/Workflow-CI.mlflow
          mlflow run . --env-manager=local

      - name: Commit training artifacts to repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p MLProject/artifacts/
          git add MLProject/artifacts/
          git diff --staged --quiet || git commit -m "chore: save training artifacts [skip ci]"
          git push

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: ./MLProject
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/heart-disease-model:latest
